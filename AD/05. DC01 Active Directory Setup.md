
# **DC1-Setup** **Active Directory Setup**

PowerShell ka use karke Active Directory domain `infosecwarrior.local` setup karein:
> [Source](https://github.com/InfoSecWarrior/Offensive-Pentesting-Lab/tree/main/AD-DACL)
* Current hostname check karein aur static IP Address configure karein:

```bash
hostname
```

* Computer ko rename karein. Hostname change karne ke liye `Rename-Computer` cmdlet ka use karein. "NewServerName" ko apne desired hostname se replace karein:

```bash
Rename-Computer -NewName "DC01" -Force
```

* Changes ko apply karne ke liye server restart karein:

```bash
Restart-Computer -Force
```

* Agar aap server ko DC01 rename karna chahte hain, toh run karein:

```bash
Rename-Computer -NewName "DC01" -Force
Restart-Computer -Force
```

* Reboot ke baad, new hostname apply ho jayega.

---

### **Step 1: Install AD DS Role**

1. Windows Server par Administrator ke roop mein PowerShell open karein.
2. Active Directory Domain Services (AD DS) role install karein:

```bash
Install-WindowsFeature AD-Domain-Services -IncludeManagementTools
```

### **Step 2: Create a New AD Forest**

- 1. PowerShell ka use karke new domain `infosecwarrior.local` create karein:

```bash
Install-ADDSForest `
-DomainName "infosecwarrior.local" `
-DomainNetbiosName "INFOWARRIOR" `
-DatabasePath "C:\Windows\NTDS" `
-LogPath "C:\Windows\NTDS" `
-SysvolPath "C:\Windows\SYSVOL" `
-InstallDns:$true `
-CreateDnsDelegation:$false `
-NoRebootOnCompletion:$false `
-DomainMode "WinThreshold" `
-ForestMode "WinThreshold" `
-Force:$true
```

*Explanation of Parameters:*

* `-DomainName` -> Domain ka FQDN
* `-DomainNetbiosName` -> Domain ke liye NetBIOS name
* `-DatabasePath` -> AD database ke liye location
* `-LogPath` -> AD log files ke liye location
* `-SysvolPath` -> SYSVOL ke liye location (policies/scripts ke liye shared folder)
* `-InstallDns` -> DNS Server install karta hai
* `-CreateDnsDelegation` -> DNS delegation creation ko skip karta hai
* `-NoRebootOnCompletion` -> Install ke baad reboot (auto-reboot $false par set karein)
* `-DomainMode -ForestMode` -> "WinThreshold" latest available domain/forest functional level set karta hai
* `-Force` -> Confirmation prompts ko skip karta hai

Setup ke baad server automatically reboot hoga agar `-NoRebootOnCompletion` `$false` par set hai.

---

### **Step 3: Managing Domain Users and Groups with PowerShell**

Create HR Users and Group in Active Directory
Create HR Group if not exists

- HR group ko HR OU me create karein agar wo pehle se exist nahi karta:

```bash
$groupName = "HR"
$ouPath = "OU=HR,DC=infosecwarrior,DC=local"

# Create OU if not exists
if (-not (Get-ADOrganizationalUnit -Filter "Name -eq 'HR'")) {
    New-ADOrganizationalUnit -Name "HR" -Path "DC=infosecwarrior,DC=local"
}

# Create Group if not exists
if (-not (Get-ADGroup -Filter {Name -eq $groupName})) {
    New-ADGroup -Name $groupName -SamAccountName $groupName -GroupCategory Security -GroupScope Global -Path $ouPath -Description "Hr Department Group"
}
```

- Create HR users and add them to the HR group:

```bash
$hrUsers = @("hr1", "hr2", "hr3")
$password = ConvertTo-SecureString "password@123" -AsPlainText -Force

foreach ($user in $hrUsers) {
    if (-not (Get-ADUser -Filter {SamAccountName -eq $user})) {
        New-ADUser -Name $user `
            -SamAccountName $user `
            -UserPrincipalName "$user@infosecwarrior.local" `
            -Path $ouPath `
            -AccountPassword $password `
            -Enabled $true `
            -PasswordNeverExpires $true `
            -Description "Hr Department User"
    }

    # Add user to Hr group and Users group
    Add-ADGroupMember -Identity "Hr" -Members $user
    Add-ADGroupMember -Identity "Users" -Members $user
}
```

- HR group ke members check karein:

```bash
Get-ADGroupMember -Identity "Hr"
```

---

### **Create Sales Users and Group in Active Directory**

- Sales group ko Sales OU me create karein agar wo pehle se exist nahi karta:

```bash
$groupName = "Sales"
$ouPath = "OU=Sales,DC=infosecwarrior,DC=local"

# Create OU if not exists
if (-not (Get-ADOrganizationalUnit -Filter "Name -eq 'Sales'")) {
    New-ADOrganizationalUnit -Name "Sales" -Path "DC=infosecwarrior,DC=local"
}

# Create Group if not exists
if (-not (Get-ADGroup -Filter {Name -eq $groupName})) {
    New-ADGroup -Name $groupName -SamAccountName $groupName -GroupCategory Security -GroupScope Global -Path $ouPath -Description "Sales Department Group"
}
```

- Sales users create karein aur unhe Sales group me add karein:

```bash
$salesUsers = @("sales1", "sales2", "sales3")
$password = ConvertTo-SecureString "password@123" -AsPlainText -Force

foreach ($user in $salesUsers) {
    if (-not (Get-ADUser -Filter {SamAccountName -eq $user})) {
        New-ADUser -Name $user `
            -SamAccountName $user `
            -UserPrincipalName "$user@infosecwarrior.local" `
            -Path $ouPath `
            -AccountPassword $password `
            -Enabled $true `
            -PasswordNeverExpires $true `
            -Description "Sales Department User"
    }

    # Add user to Sales group and Users group
    Add-ADGroupMember -Identity "Sales" -Members $user
    Add-ADGroupMember -Identity "Users" -Members $user
}
```

- Sales group ke members check karein:

```bash
Get-ADGroupMember -Identity "Sales"
```

---

### **Create Manager (MGR) Users and Group in Active Directory**

- Manager group ko Manager OU me create karein agar wo pehle se exist nahi karta:

```bash
$groupName = "MGR"
$ouPath = "OU=Manager,DC=infosecwarrior,DC=local"

# Create OU if not exists
if (-not (Get-ADOrganizationalUnit -Filter "Name -eq 'Manager'")) {
    New-ADOrganizationalUnit -Name "Manager" -Path "DC=infosecwarrior,DC=local"
}

# Create Group if not exists
if (-not (Get-ADGroup -Filter {Name -eq $groupName})) {
    New-ADGroup -Name $groupName -SamAccountName $groupName -GroupCategory Security -GroupScope Global -Path $ouPath -Description "Manager Department Group"
}

```

- Manager users create karein aur unhe Manager group me add karein:

```bash
$mgrUsers = @("mgr1", "mgr2", "mgr3")
$password = ConvertTo-SecureString "password@123" -AsPlainText -Force

foreach ($user in $mgrUsers) {
    if (-not (Get-ADUser -Filter {SamAccountName -eq $user})) {
        New-ADUser -Name $user `
            -SamAccountName $user `
            -UserPrincipalName "$user@infosecwarrior.local" `
            -Path $ouPath `
            -AccountPassword $password `
            -Enabled $true `
            -PasswordNeverExpires $true `
            -Description "Manager Department User"
    }

    # Add user to MGR group and Users group
    Add-ADGroupMember -Identity "Mgr" -Members $user
    Add-ADGroupMember -Identity "Users" -Members $user
}
```

- MGR group ke members check karein:

```bash
Get-ADGroupMember -Identity "mgr"
```

---

### **Step 4: Configure DNS and Networking**

- Confirm DNS Server Settings:

```bash
Get-DnsServerZone
```

- Ensure karein ki AD DNS work kar raha hai:

```bash
Resolve-DnsName infosecwarrior.local
```

- Confirm karein ki AD setup work kar raha hai ye run karke:

```bash
Get-ADDomainController
```

---

### **PowerShell Script for AD Pentest Lab Setup**

* Create AD users and groups
* ACL/ACE abuse ke liye permissions set karein
* Attack scenarios configure karein (Kerberoasting, DCSync, Golden Ticket, etc.)
* Ek realistic AD pentest environment setup karein
* Notepad me code script ko dalker AD_Lab_Setup.ps1 is name se save karwa lenge 

```bash
AD_Lab_Setup.ps1
```
```bash
# Define Variables
$DomainName = "infosecwarrior.local"
$OU = "OU=LabUsers,DC=infosecwarrior,DC=local"
$SecurePassword = ConvertTo-SecureString "password@123" -AsPlainText -Force

# Create OU if not exists
if (!(Get-ADOrganizationalUnit -Filter {Name -eq "LabUsers"})) {
    New-ADOrganizationalUnit -Name "LabUsers" -Path "DC=infosecwarrior,DC=local" -ProtectedFromAccidentalDeletion $false
}

# Create Users (if not exists)
$Users = @(
    "ankur","pooja","rahul","ankit","vikas","amit","karan","rohit","deepak","manoj",
    "sahil","arjun","priya","nidhi","kiran","pallavi","sunil","tanya","harsh","sneha",
    "rohan","mohit","ajay","anil","sumit","shivam","ravi","shweta","neha","alok",
    "sachin","yash","harshita","piyush","ananya","ashok","shubham","ankita","prateek",
    "rishabh","abhinav","vivek","vaibhav","kritika","sagar","mayank","deepali","aakash"
)

foreach ($User in $Users) {
    if (!(Get-ADUser -Filter {SamAccountName -eq $User})) {
        New-ADUser -Name $User -SamAccountName $User -UserPrincipalName "$User@$DomainName" `
            -Path $OU -AccountPassword $SecurePassword -Enabled $true
    }
}

# Create sql_services account if not exists
if (!(Get-ADUser -Filter {SamAccountName -eq "sql_services"})) {
    New-ADUser -Name "SQL Services" -SamAccountName "sql_services" -UserPrincipalName "sql_services@$DomainName" `
        -Path $OU -AccountPassword $SecurePassword -Enabled $true
}

# Create Groups (if not exists)
$Groups = @("DnsAdmins", "SQL Admins", "Domain Admins", "Enterprise Admins", "Schema Admins")

foreach ($Group in $Groups) {
    if (!(Get-ADGroup -Filter {Name -eq $Group})) {
        New-ADGroup -Name $Group -SamAccountName $Group -GroupScope Global -Path $OU
    }
}

# Add Users to Groups
Add-ADGroupMember -Identity "Domain Admins" -Members "ankit", "sahil"
Add-ADGroupMember -Identity "DnsAdmins" -Members "sneha", "vikas"
Add-ADGroupMember -Identity "SQL Admins" -Members "ankur", "pooja"
Add-ADGroupMember -Identity "Enterprise Admins" -Members "karan"
Add-ADGroupMember -Identity "Schema Admins" -Members "deepak"

# Assign ACLs/ACEs for Abuse Scenarios

$Permissions = @(
    @{User="ankur";Permission="ForceChangePassword";GUID="00299570-246d-11d0-a768-00aa006e0529"}
    @{User="sneha";Permission="AddMembers";GUID="bf9679c0-0de6-11d0-a285-00aa003049e2"}
    @{User="vikas";Permission="GenericAll";GUID="bf9679c0-0de6-11d0-a285-00aa003049e2"}
    @{User="amit";Permission="GenericWrite";GUID="bf9679c0-0de6-11d0-a285-00aa003049e2"}
    @{User="karan";Permission="WriteOwner"}
    @{User="rohit";Permission="WriteDACL"}
    @{User="deepak";Permission="AllExtendedRights"}
)

foreach ($Perm in $Permissions) {
    $User = $Perm.User

    if (Get-ADUser -Filter {SamAccountName -eq $User}) {
        $acl = Get-Acl "AD:\CN=$User,$OU"

        $ace = switch ($Perm.Permission) {
            "ForceChangePassword" { New-Object System.DirectoryServices.ActiveDirectoryAccessRule(
                (New-Object System.Security.Principal.NTAccount("$DomainName\$User")),
                "ExtendedRight",
                "Allow",
                [GUID]$Perm.GUID
            )}
            "AddMembers" { New-Object System.DirectoryServices.ActiveDirectoryAccessRule(
                (New-Object System.Security.Principal.NTAccount("$DomainName\$User")),
                "ExtendedRight",
                "Allow",
                [GUID]$Perm.GUID
            )}
            "GenericAll" { New-Object System.DirectoryServices.ActiveDirectoryAccessRule(
                (New-Object System.Security.Principal.NTAccount("$DomainName\$User")),
                "GenericAll",
                "Allow"
            )}
            "GenericWrite" { New-Object System.DirectoryServices.ActiveDirectoryAccessRule(
                (New-Object System.Security.Principal.NTAccount("$DomainName\$User")),
                "WriteProperty",
                "Allow"
            )}
            "WriteOwner" { New-Object System.DirectoryServices.ActiveDirectoryAccessRule(
                (New-Object System.Security.Principal.NTAccount("$DomainName\$User")),
                "WriteOwner",
                "Allow"
            )}
            "WriteDACL" { New-Object System.DirectoryServices.ActiveDirectoryAccessRule(
                (New-Object System.Security.Principal.NTAccount("$DomainName\$User")),
                "WriteDacl",
                "Allow"
            )}
            "AllExtendedRights" { New-Object System.DirectoryServices.ActiveDirectoryAccessRule(
                (New-Object System.Security.Principal.NTAccount("$DomainName\$User")),
                "ExtendedRight",
                "Allow"
            )}
        }

        $acl.AddAccessRule($ace)
        Set-Acl -Path "AD:\CN=$User,$OU" -AclObject $acl -Confirm:$false
    }
}

#
# Attack Scenarios
#

# Kerberoasting
Set-ADUser -Identity "sql_services" -ServicePrincipalNames @{Add="MSSQLSvc/dc1.infosecwarrior.local:50111"}

# AS-REP Roasting
Set-ADUser -Identity "rohan" -KerberosEncryptionType "None" -Confirm:$false

# Abuse DnsAdmins (Run DNS as SYSTEM)
Set-ADGroup -Identity "DnsAdmins" -ManagedBy "sneha"

# DCSync (Replication permissions)
$DomainObject = Get-ADDomain
$acl = Get-Acl "AD:\$($DomainObject.DistinguishedName)"
$ace = New-Object System.DirectoryServices.ActiveDirectoryAccessRule(
    (New-Object System.Security.Principal.NTAccount("$DomainName\ankit")),
    "ExtendedRight",
    "Allow",
    [GUID]"89e95b76-444d-4c62-991a-0facbeda640c"
)
$acl.AddAccessRule($ace)
Set-Acl -Path "AD:\$($DomainObject.DistinguishedName)" -AclObject $acl

# Public SMB Share
$PublicPath = "C:\Public"
if (!(Test-Path $PublicPath)) { New-Item -Path $PublicPath -ItemType Directory }
New-SmbShare -Name "Public" -Path $PublicPath -FullAccess Everyone

# Disable Firewall
Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled False

#
Write-Host "[+] AD Pentest Lab Setup Complete!" -ForegroundColor Green
```

---

### **Configured Attacks:**

Setup complete hone ke baad ye attacks configure ho jayenge:

* Abusing ACLs/ACEs
* Kerberoasting
* AS-REP Roasting
* DCSync
* Golden Ticket
* Silver Ticket
* Pass-the-Hash
* Pass-the-Ticket
* SMB Signing Disabled
* WinRM Misconfig
* Anonymous LDAP Query
* Public SMB Share
* Password in User Comment
* Zerologon

### **How to Run:**

1. PowerShell ko Administrator ke roop me open karein.
2. Upar diye gaye script ko `AD_Lab_Setup.ps1` naam se save karein.
3. Execute karein:

```bash
.\AD_Lab_Setup.ps1
```

### **SPN Configuration**

| Service Account | SPN | Port |
| --- | --- | --- |
| sql_services | MSSQLSvc/dc1.infosecwarrior.local | 50111 |

---
---

# Active Directory – Anonymous Access + User Creation + RDP Access

### 1) Anonymous Access to Active Directory

Reference Link:
[https://activedirectoryfaq.com/2016/09/anonymous-access](https://activedirectoryfaq.com/2016/09/anonymous-access)

Anonymous access ka matlab hota hai bina authentication ke AD objects ko read karna. Ye normally secure environments me disabled hota hai. Agar lab setup me enable karna ho to Directory Service attribute modify karna padta hai.

- Step 1: Open ADSI Edit

Domain Controller par Domain Admin account se login karo.

Run command:

```bash
adsiedit.msc
```

---

- Step 2: Connect to Configuration Partition

ADSI Edit me connect karo:

Path:

```
CN=Directory Service,CN=Windows NT,CN=Services,CN=Configuration,DC=<my domain>
```

Yaha `<my domain>` ko apne domain se replace karo.

---

- Step 3: Modify Attribute

Directory Service ke properties me jao → Attribute Editor open karo.

Attribute:

```
dSHeuristics
```

Value set karo:

```
0000002
```

Ye anonymous access enable karta hai.

---

### 2) Enabling Anonymous Reading (Permission Grant)


Sirf attribute change karna kaafi nahi hota. Anonymous account ko read permission bhi deni hoti hai specific OU ya domain par.

PowerShell example:

```bash
dsacls "OU=Users,DC=infosecwarrior,DC=local" /G "ANONYMOUS LOGON:R"
```

Explanation:

* `dsacls` → ACL modify karta hai
* `/G` → Grant permission
* `ANONYMOUS LOGON:R` → Read permission

---

### 3) Add New User in infosecwarrior.local Domain

- Step 1: Create New Domain User

Domain Controller par PowerShell open karo aur run karo:

```bash
New-ADUser -Name "John Doe" -GivenName "John" -Surname "Doe" -SamAccountName "jdoe" -UserPrincipalName "jdoe@infosecwarrior.local" -Path "CN=Users,DC=infosecwarrior,DC=local" -AccountPassword (ConvertTo-SecureString "Password123!" -AsPlainText -Force) -Enabled $true
```

### Parameters Short Explanation

* `-Name` → Full name
* `-SamAccountName` → Login username
* `-UserPrincipalName` → UPN format login
* `-Path` → OU location
* `-AccountPassword` → Password set karta hai
* `-Enabled $true` → Account activate karta hai

---

### 4) Add User to Remote Desktop Users Group

User ko RDP access dene ke liye group me add karna zaroori hai.

Command:

```bash
Add-ADGroupMember -Identity "Remote Desktop Users" -Members "jdoe"
```

Explanation:

* `-Identity` → Group name
* `-Members` → User jo add karna hai

---

### 5) Verify User is Added in Group

Check karo user group me add hua ya nahi:

```bash
Get-ADGroupMember -Identity "Remote Desktop Users"
```

Agar `jdoe` list me dikh raha hai to successfully add ho gaya.

---

### 6) Allow RDP Access via Group Policy (Optional but Recommended)

Kabhi kabhi sirf group membership kaafi nahi hota. GPO me RDP login right allow karna padta hai.

- Step 1: Open Group Policy Management

```bash
gpmc.msc
```

---

- Step 2: Navigate to Path

```
Computer Configuration → Policies → Windows Settings → Security Settings → Local Policies → User Rights Assignment
```

---

- Step 3: Modify Policy

Find setting:

```
Allow log on through Remote Desktop Services
```

Agar `Remote Desktop Users` nahi hai to add karo.

---

- Step 4: Apply Policy

```bash
gpupdate /force
```

---

### 7) Final RDP Login Check

Login details:

Username:

```
infosecwarrior\jdoe
```

Password:

```
Password123!
```

RDP Target:

```
infosecwarrior.local
```

Ab `jdoe` RDP se login kar sakta hai.

---

### Summary Flow (Step-by-Step Execution Order)

1. `adsiedit.msc`
2. dSHeuristics = 0000002
3. `dsacls` permission grant
4. `New-ADUser`
5. `Add-ADGroupMember`
6. `Get-ADGroupMember`
7. `gpmc.msc`
8. RDP policy allow
9. `gpupdate /force`
10. RDP login test

---
